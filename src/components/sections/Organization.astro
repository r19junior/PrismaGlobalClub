---
import Container from '../ui/Container.astro';
import PersonCard from '../cards/PersonCard.astro';
import { orgChart } from '../../data/organization';
---

<section id="organizacion" class="py-24 lg:py-32 bg-prisma-warm" aria-labelledby="org-title">
  <Container>
    <div class="max-w-xl mb-16">
      <span class="inline-block text-xs font-bold uppercase tracking-[0.2em] text-prisma-red mb-4">
        Estructura
      </span>
      <h2 id="org-title" class="text-3xl sm:text-4xl lg:text-5xl font-black leading-tight tracking-tight text-prisma-dark mb-3">
        Las personas detrás de Prisma
      </h2>
      <p class="text-prisma-gray">Una estructura que convierte propósito en acción.</p>
    </div>

    <!-- Organigrama Desktop (Estilo Mapa Moderno) -->
    <div class="hidden lg:flex flex-col items-center mt-10">
      {orgChart.map((level, li) => (
        <div class="flex flex-col items-center w-full">
          
          <!-- Línea vertical conectora desde el nivel anterior -->
          {li > 0 && (
            <div class="w-[2px] h-12 bg-prisma-border"></div>
          )}

          <!-- Etiqueta del Nivel (Pill badge) -->
          <div class="bg-prisma-red text-white font-bold px-8 py-3 text-[11px] uppercase tracking-[0.2em] rounded-full shadow-md relative z-10">
            {level.label}
          </div>

          <!-- Línea vertical conectora hacia los departamentos -->
          <div class="w-[2px] h-10 bg-prisma-border"></div>

          <!-- Contenedor de Departamentos (Hijos) -->
          <div class="flex justify-center relative w-full">
            {level.departments.map((dept, index) => (
              <div class="relative flex flex-col items-center px-4 w-60">
                <!-- Línea horizontal conectora (solo si hay más de 1 departamento) -->
                {level.departments.length > 1 && (
                  <div class="absolute top-0 h-[2px] bg-prisma-border"
                       class:list={[
                         index === 0 ? "left-1/2 right-0" : 
                         index === level.departments.length - 1 ? "left-0 right-1/2" : 
                         "left-0 right-0"
                       ]}>
                  </div>
                )}
                
                <!-- Línea vertical individual que baja a la tarjeta -->
                <div class="w-[2px] h-6 bg-prisma-border"></div>
                
                <!-- Tarjeta de Departamento Moderna -->
                <div class="bg-white rounded-2xl border border-gray-100 shadow-[0_8px_30px_rgb(0,0,0,0.06)] overflow-hidden w-full transition-transform hover:-translate-y-1 duration-300">
                   <!-- Cabecera del departamento -->
                   <div class="bg-gray-50/80 px-4 py-3.5 border-b border-gray-100 flex items-center justify-center min-h-[3.5rem]">
                     <span class="text-[10px] font-black uppercase tracking-widest text-prisma-red text-center leading-tight">
                       {dept.name}
                     </span>
                   </div>
                   <!-- Lista de miembros -->
                   <div class="p-5 space-y-4">
                     {dept.members.map((m) => (
                       <div class="flex flex-col items-center text-center">
                         <span class="text-[14px] font-bold text-prisma-dark leading-snug">{m.name}</span>
                         <span class="text-[10px] uppercase tracking-wider text-prisma-gray mt-1">{m.role}</span>
                       </div>
                     ))}
                   </div>
                </div>
              </div>
            ))}
          </div>
        </div>
      ))}
    </div>

    <!-- Organigrama Móvil: Acordeones HTML nativos -->
    <div class="lg:hidden space-y-3">
      {orgChart.map((level) => (
        <details class="group bg-prisma-white rounded-xl border border-prisma-border overflow-hidden">
          <summary class="flex items-center justify-between px-5 py-4 cursor-pointer list-none select-none">
            <div>
              <span class="text-xs font-black uppercase tracking-widest text-prisma-red block mb-0.5">Nivel {level.level}</span>
              <span class="text-sm font-bold text-prisma-dark">{level.label}</span>
            </div>
            <svg
              class="w-4 h-4 text-prisma-gray transition-transform duration-200 group-open:rotate-180"
              viewBox="0 0 20 20" fill="currentColor" aria-hidden="true"
            >
              <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd" />
            </svg>
          </summary>
          <div class="px-5 pb-4 space-y-4 border-t border-prisma-border">
            {level.departments.map((dept) => (
              <div class="pt-3">
                <p class="text-xs font-bold uppercase tracking-wider text-prisma-gray-light mb-2">{dept.name}</p>
                <div class="space-y-0.5">
                  {dept.members.map((person) => (
                    <PersonCard {...person} />
                  ))}
                </div>
              </div>
            ))}
          </div>
        </details>
      ))}
    </div>
  </Container>
</section>
